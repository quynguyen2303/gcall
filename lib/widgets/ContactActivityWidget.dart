import 'package:flutter/material.dart';

class ContactActivityWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is activities'),
    );
  }
}