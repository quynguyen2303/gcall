import 'package:flutter/material.dart';

class ContactDetailBodyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is a body'),
    );
  }
}