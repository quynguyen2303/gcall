import 'package:flutter/material.dart';

class CallHistory extends StatelessWidget {

  static const routeName = '/CallHistory';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
        child: Center(child: Text('Call History Page')),
      ),
    );
  }
}
